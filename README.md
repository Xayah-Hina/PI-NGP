# PI-NGP

```shell
python -m pip install --upgrade pip setuptools wheel
python -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
python -m pip install ninja tqdm pyyaml opencv-python matplotlib tyro torch-ema imageio imageio[ffmpeg] tensorboard scipy pyglet<2 trimesh standard-imghdr
```